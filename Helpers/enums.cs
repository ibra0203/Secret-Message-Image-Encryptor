using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace MessageDecoder
{
    public enum IMAGE_UPLOAD_RESULT { SUCCESS, FAILED, BIGFILE, DIMNOTEQUAL, NOTBMP }
    public enum SITE_PAGES { NONE, MAIN, ENCODE, DECODE, DISPLAYIMG}
    public class enums
    {
    }
}